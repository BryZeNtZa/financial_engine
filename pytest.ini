[pytest]
testpaths = financial_engine/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    # flask-restx imports the deprecated jsonschema.RefResolver (third-party,
    # not under our control). Silence just this warning.
    ignore:jsonschema\.RefResolver is deprecated.*:DeprecationWarning
